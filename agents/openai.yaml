interface:
  display_name: "VASP 2D Monolayer"
  short_description: "Audit and run monolayer 2D VASP workflows on lilin."
  brand_color: "#2563EB"
  default_prompt: "Use $vasp-2d-monolayer to audit this POSCAR, list the monolayer VASP calculations needed, and wait for confirmation before submitting Slurm jobs."

policy:
  allow_implicit_invocation: true
