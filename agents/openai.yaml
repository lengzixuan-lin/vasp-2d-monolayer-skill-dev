interface:
  display_name: "VASP 2D Monolayer"
  short_description: "Audit and plan single-layer 2D VASP workflows with explicit server-safety gates."
  brand_color: "#2563EB"
  default_prompt: "Use $vasp-2d-monolayer only for single-layer 2D monolayer POSCARs, monolayer VASP workflow planning, or this skill's maintenance docs. Read local references first, list calculation risks, and wait for explicit confirmation before SSH, Slurm submission, server dry-run, remote write, real task edits, or installed-skill sync."

policy:
  allow_implicit_invocation: true
