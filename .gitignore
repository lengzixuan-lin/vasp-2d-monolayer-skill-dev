# Runtime logs
*.log
devspace.log
devspace.err.log

# Python caches
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local secrets and machine-specific config
.env
.env.*
*.pem
*.key
*.crt
id_rsa*
id_ed25519*

# VASP and remote-compute outputs
OUTCAR
OSZICAR
WAVECAR
CHGCAR
CHG
DOSCAR
EIGENVAL
PROCAR
vasprun.xml
XDATCAR
IBZKPT
REPORT
slurm-*.out
*.err
*.out

# Reference bundles and third-party materials
#
# Keep full reference bundles local by default. Curated summaries should usually
# live under `docs/`; if a small file must stay under this directory, stage it
# explicitly with `git add -f <file>` after user approval.
vasp_references资料/**
